%h1 フィードバック一覧

= link_to '新規登録', new_teacher_lesson_reservation_feedback_path(@lesson_reservation), class: 'btn btn-outline-primary'
%table.table.table-striped.mt-3
  %thead
    %th 内容
    %th 登録日時
    %th
  %tbody
    - @feedbacks.each do |feedback|
      %tr
        %td= simple_format(feedback.body)
        %td= feedback.created_at.strftime('%Y/%m/%d %H:%M:%S')
        %td
          = link_to '編集',
                    edit_teacher_lesson_reservation_feedback_path(@lesson_reservation, feedback),
                    class: 'btn btn-outline-primary'
          = link_to '削除',
                    teacher_lesson_reservation_feedback_path(@lesson_reservation, feedback),
                    method: :delete,
                    class: 'btn btn-outline-danger',
                    data: { confirm: '削除しますか？' }

= link_to '戻る', teacher_lesson_reservations_path, class: 'btn btn-outline-secondary'
